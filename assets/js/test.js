// Тестовий файл — для перевірки функцій або експериментів

function sum(a, b) {
  return a + b;
}

function logTestResult() {
  const result = sum(2, 3);
  console.log(`Результат тесту: ${result}`); // Очікуємо 5
}

logTestResult();